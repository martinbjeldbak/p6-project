\begin{tikzpicture}
  \pgfplotsset{%
    %grid=major, % show grid aligned on major ticks
    grid style={dashed,gray!30}, % draw grid behind plot
    %scale only axis,
  }
\begin{axis}[
    ylabel         = {Diversity},
    xlabel         = {Generation},
    legend columns = 2,
    legend style={%
      at={(0,1.2)},
      anchor=north west 
    },
  ]
  \addplot[smooth, no markers, color=navy]
    table[col sep=comma, x index=0, y index=1] {data/replacementrule_game/greedy_snake.csv};
  \addplot[smooth, no markers, color=blue]
    table[col sep=comma, x index=0, y index=1] {data/replacementrule_game/aerr_snake.csv};
  \addplot[smooth, no markers, color=aqua]
    table[col sep=comma, x index=0, y index=1] {data/replacementrule_game/sperr_snake.csv};
  \addplot[smooth, no markers, color=teal]
    table[col sep=comma, x index=0, y index=1] {data/replacementrule_game/eerr_snake.csv};
  \legend{Greedy, Ancestor Elitism, Single Parent Elitism, Explore Exploit}
\end{axis}
\end{tikzpicture}
